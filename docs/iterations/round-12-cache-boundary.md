# 第 12 轮：HTTP 缓存时间边界

## 更新内容

- 缓存到期时刻不再被当作新鲜数据。
- stale 缓存窗口结束时刻不再返回旧值。
- undefined 不写入缓存，避免占用条目但永远无法命中。
- 增加 TTL 和 HTTP 缓存边界回归测试。

## 边界行为

- 有效缓存继续按原有 TTL 命中。
- 允许 stale 时只在正式 TTL 过期后且 stale 窗口未结束时返回旧值。
