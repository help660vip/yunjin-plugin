# 第 7 轮：审计记录安全性

## 更新内容

- 审计记录统一隐藏 token、secret、password、cookie 等敏感键。
- 字符串、数组、对象深度和对象键数增加上限。
- 对 BigInt、函数、Symbol、日期和 Error 做可序列化降级，避免一条异常记录导致整个链路失败。
- 新增审计文件回归测试，覆盖脱敏、超长文本、超长数组和深层对象。

## 边界行为

- 审计日志优先保留操作和身份识别信息，超限元数据被截断。
- 敏感键不会保留原始值，不可序列化值使用中文占位文本。
