# 第 10 轮：渲染异常降级

## 更新内容

- 渲染器返回空字符串、空数组、null 或 undefined 时统一回退安全文本。
- 保留非空渲染字符串、消息数组和明确消息对象。
- 渲染器异常或 onError 回调异常不会遮蔽文本降级。
- 新增空结果、异常回调和有效消息对象回归测试。

## 边界行为

- 本轮不改变渲染限流和 HTML 清理，仅改善返回值和异常路径。
- 消息对象仍由上层消息适配器负责发送。
