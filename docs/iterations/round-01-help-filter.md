# 第 1 轮：帮助关键词筛选

## 变更目的

让用户可以从全部帮助中快速定位能力，保留原有按领域查看方式。

## 行为契约

- 不传参数：返回所有当前可用能力。
- 传入领域：只返回该领域能力。
- 传入关键词：按能力编号、命令、名称和领域匹配。
- 无匹配：返回中文提示并记录帮助查询审计。

## 风险边界

- 搜索词受参数长度和能力清单上限约束。
- 只返回已经由注册表暴露的能力，不泄露内部路径或参考来源。

## 验收

- 覆盖正常筛选、查看别名和空结果提示。
