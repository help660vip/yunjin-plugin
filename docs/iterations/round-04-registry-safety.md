# 第 4 轮：能力注册表健壮性

## 更新内容

- 注册表启动时校验每个 manifest 的编号、主命令和命令列表。
- 重复能力编号或主命令时在启动阶段报错，不再静默覆盖。
- 对清单进行标准化复制，防止外部修改注册表内部数组。
- 增加重复编号、重复命令、无效清单和输出隔离回归测试。

## 边界行为

- 清单不是数组时按空清单处理，但任何无效项都会拒绝启动。
- 注册表仍按能力编号稳定输出，不改变用户命令。

## 风险与取舍

- 未按约定编写的外部能力会在启动时明确失败，优先保证能力路由正确。
