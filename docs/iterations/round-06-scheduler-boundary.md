# 第 6 轮：调度器边界稳定性

## 更新内容

- 新建任务统一从 scheduled 状态开始，不允许外部请求直接写入终态。
- 重试次数和最大尝试次数统一取整，避免小数输入导致多执行。
- 过期运行时间先回退到当前时间，再参与稳定 ID 计算。
- 增加新任务状态、过期时间和重试次数回归测试。

## 边界行为

- 从存储恢复的任务保留其终态，只有新建任务强制使用 scheduled。
- 过期任务仍可立即执行，不题外改变调度语义。
