# 第 11 轮：订阅 URL 规范化

## 更新内容

- 18 号通用订阅对 HTTP/HTTPS 目标统一转小写主机、排序查询参数、去除 hash 和根路径尾斜杠。
- 19 号 RSS 订阅在安全 URL 校验后使用同一规范化形式去重。
- 非 URL 订阅目标保留原始文本，不强制改变业务语义。
- 新增通用订阅和 RSS 重复 URL 回归测试。

## 边界行为

- 账号密码 URL 不进入规范化结果，仍由网络安全校验决定是否允许。
- 规范化只用于同一作用域内的订阅去重，不改变权限和存储作用域。
