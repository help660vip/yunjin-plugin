# 第 9 轮：JSON 资料仓库稳定性

## 更新内容

- 本地 JSON 仓库的临时文件名改为随机 UUID，避免多个实例同时写入时碰撞。
- 序列化、写入或重命名任一步失败都会清理临时文件后重新抛出错误。
- 保留原有进程内锁，并增加多个仓库实例并发更新回归。
- 增加 BigInt 序列化失败后临时文件清理回归。

## 边界行为

- 序列化失败仍会向调用方抛出，不会静默丢弃写入。
- 临时文件只在完整写入后原子替换目标文件。
